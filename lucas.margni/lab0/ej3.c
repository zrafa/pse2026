/* ¿Cuántos bits puede almacenar una variable declarada como int en computadoras de arquitecturas diferentes: 
    arquitecturas de 8bits, 32bits y 64bits? */

#include <stdio.h>

int main() {
    printf("En una arquitectura de 8 bits, una variable int ocupa 2 bytes (8 bits)\n");
    printf("En una arquitectura de 32 y 64 bits, una variable int ocupa 4 bytes (32 bits)\n");

    return 0;
}