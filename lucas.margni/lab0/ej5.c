/* ¿Cuál es el rango numérico de i y j? */

#include <stdio.h>

int main() {
    char i;
    unsigned char j;

    /* Ambos tipos de datos pueden almacenar valores que se puedan escribir en binario usando 8 bits.
    Sin embargo, cambian en la forma en la que representan dichos valores.
    char guarda valores tanto positivos como negativos, en un rango de -128 a 127.
    unsigned int guarda valores solamente positivos, en un rango de 0 a 255. */

    return 0;
}