//Los tipos de datos básicos en C son int (para números enteros), que puede modificarse con long o short, float y double (para números de punto flotante de 4 y 8 bytes respectivamente) y char (para caracteres de 1 byte). Todos estos se pueden modificar con signed, unsigned para ajustar rangos y tamaño en memoria.

int main() {
    int numEntero; //numero entero de 4 bytes, puede ser long int para que sea de 8 bytes y short int para que sea de 2 bytes
    float numReal; // numeros con presicion simple de 4 bytes
    double numRealDoble; // numeros con presicion doble de 8 bytes, en algunos casos se puede utilizar long double
    char caracter; // un caracter de 1 byte
}