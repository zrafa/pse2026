int main() {
    char i;
    unsigned char j;

    /* Tanto char como unsigned char almacenan 1 byte en memoria, es decir, 8 bits. La principal diferencia está en los rangos de representación ya que el unsigned no almacena números negativos
    El rango de i es de -128 a 127.
    El rango de j es de 0 a 255. */
}